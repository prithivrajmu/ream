# Guardrails

## Signs

### Sign: Bounded Loop Only

- Use Ralph only in bounded iterations through `prstack loop`

### Sign: prstack Owns Workflow

- Keep `prstack` as the workflow layer; Ralph is an execution loop inside it
