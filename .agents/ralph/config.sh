PRD_PATH="${PRD_PATH:-.agents/tasks/prd.json}"
PROGRESS_PATH="${PROGRESS_PATH:-.ralph/progress.md}"
GUARDRAILS_PATH="${GUARDRAILS_PATH:-.ralph/guardrails.md}"
ERRORS_LOG_PATH="${ERRORS_LOG_PATH:-.ralph/errors.log}"
ACTIVITY_LOG_PATH="${ACTIVITY_LOG_PATH:-.ralph/activity.log}"
TMP_DIR="${TMP_DIR:-.ralph/.tmp}"
RUNS_DIR="${RUNS_DIR:-.ralph/runs}"
GUARDRAILS_REF="${GUARDRAILS_REF:-.agents/ralph/references/GUARDRAILS.md}"
CONTEXT_REF="${CONTEXT_REF:-.agents/ralph/references/CONTEXT_ENGINEERING.md}"
ACTIVITY_CMD="${ACTIVITY_CMD:-.agents/ralph/log-activity.sh}"
AGENT_CMD="${AGENT_CMD:-codex exec --yolo --skip-git-repo-check -}"
PRD_AGENT_CMD="${PRD_AGENT_CMD:-codex exec --yolo --skip-git-repo-check -}"
AGENTS_PATH="${AGENTS_PATH:-AGENTS.md}"
PROMPT_BUILD="${PROMPT_BUILD:-.agents/ralph/PROMPT_build.md}"
NO_COMMIT="${NO_COMMIT:-false}"
MAX_ITERATIONS="${MAX_ITERATIONS:-2}"
STALE_SECONDS="${STALE_SECONDS:-900}"
