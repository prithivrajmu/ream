# Tasks

## Now

- [ ] Split renderer into focused components and hooks without changing behavior
- [ ] Harden JSON import validation, schema checks, and restore confirmation semantics
- [ ] Add lint/format tooling and include it in prstack QA

## Next

- [ ] Add manual time entry create/edit/delete
- [ ] Add Electron UI smoke coverage for overlay, start/stop, note, and export
- [ ] Replace overlay/main polling with event-based refresh after repository writes

## Later

- [ ] Add macOS packaging guidance and build config
- [ ] Add private GitHub remote setup documentation
- [ ] Evaluate SQLite migration only if IndexedDB backup/restore becomes unreliable
